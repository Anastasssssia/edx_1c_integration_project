# edx_1c_integration_project

Проект реализует промежуточный сервис-адаптер, который получает финансовые операции из API edX, преобразует их в унифицированный JSON-формат и передает в 1С через HTTP-сервис или сохраняет пакетные выгрузки в файлы `export_YYYYMMDD.json`.
